import React from "react";
import ReactDOM from "react-dom/client";
import { PaginaWidget } from "./paginas/PaginaWidget";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <PaginaWidget />
  </React.StrictMode>,
);
